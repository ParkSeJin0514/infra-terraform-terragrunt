# ============================================================================
# Terraform Apply
# ============================================================================
# 수동 실행으로 레이어 선택하여 Apply
# - all: foundation → compute → bootstrap 순차 실행
# - foundation / compute / bootstrap: 개별 레이어만 실행
# ============================================================================

name: Terraform Apply

on:
  workflow_dispatch:
    inputs:
      layer:
        description: '적용할 레이어 선택'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - foundation
          - compute
          - bootstrap

env:
  AWS_REGION: ap-northeast-2
  TF_VERSION: 1.9.0
  TG_VERSION: 0.54.0

# 동시에 하나만 실행 (순차 처리)
concurrency:
  group: terraform-apply
  cancel-in-progress: false

jobs:
  apply:
    name: Terraform Apply (${{ github.event.inputs.layer }})
    runs-on: ubuntu-latest
    
    # OIDC 토큰 발급 권한
    permissions:
      id-token: write
      contents: read
    
    steps:
      # ========================================
      # 1. 체크아웃
      # ========================================
      - name: Checkout
        uses: actions/checkout@v4

      # ========================================
      # 2. AWS 인증 (OIDC)
      # ========================================
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # ========================================
      # 3. Terraform 설치
      # ========================================
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      # ========================================
      # 4. Terragrunt 설치
      # ========================================
      - name: Setup Terragrunt
        run: |
          wget -q https://github.com/gruntwork-io/terragrunt/releases/download/v${{ env.TG_VERSION }}/terragrunt_linux_amd64
          chmod +x terragrunt_linux_amd64
          sudo mv terragrunt_linux_amd64 /usr/local/bin/terragrunt
          terragrunt --version

      # ========================================
      # 5. SSH Key 생성
      # ========================================
      - name: Create SSH Key
        run: |
          mkdir -p keys
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > keys/test.pub

      # ========================================
      # 6. Apply Foundation
      # ========================================
      - name: Apply Foundation
        if: github.event.inputs.layer == 'all' || github.event.inputs.layer == 'foundation'
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
        run: |
          echo "🏗️ Foundation Layer Apply 시작..."
          cd foundation && terragrunt apply --terragrunt-non-interactive -auto-approve
          echo "✅ Foundation Layer 완료"

      # ========================================
      # 7. Apply Compute
      # ========================================
      - name: Apply Compute
        if: github.event.inputs.layer == 'all' || github.event.inputs.layer == 'compute'
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
        run: |
          echo "🖥️ Compute Layer Apply 시작..."
          cd compute && terragrunt apply --terragrunt-non-interactive -auto-approve
          echo "✅ Compute Layer 완료"

      # ========================================
      # 8. Apply Bootstrap
      # ========================================
      - name: Apply Bootstrap
        if: github.event.inputs.layer == 'all' || github.event.inputs.layer == 'bootstrap'
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
        run: |
          echo "🚀 Bootstrap Layer Apply 시작..."
          cd bootstrap && terragrunt apply --terragrunt-non-interactive -auto-approve
          echo "✅ Bootstrap Layer 완료"

      # ========================================
      # 9. 완료 메시지
      # ========================================
      - name: Apply Complete
        run: |
          echo "=============================================="
          echo "🎉 Terraform Apply 완료!"
          echo "=============================================="
          echo "적용된 레이어: ${{ github.event.inputs.layer }}"